Meteor.publish('rooms',function(){
  return Rooms.find();
});

Meteor.publish('userPresence', function(){
  return Meteor.presences.find({});
});

Meteor.publish('userData', function() {
  return Meteor.users.find({_id: this.userId}, { username: 1, profile: 1 } );
});
